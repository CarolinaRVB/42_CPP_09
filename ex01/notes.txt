can have a std::vector container that stores the parsed command
Then build linked lists with each node containnig the calculation to make:
    - first already has calculation
    - next is performed on before, etc

    Adviced to use stack for push operations